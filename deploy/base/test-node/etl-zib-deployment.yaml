apiVersion: apps/v1
kind: Deployment
metadata:
  name: test-etl-zib
  namespace: dh-health
  labels:
    app: test-etl-zib
spec:
  replicas: 1
  selector:
    matchLabels:
      app: test-etl-zib
  template:
    metadata:
      labels:
        app: test-etl-zib
    spec:
      containers:
      - name: test-etl-zib
        image: docker-health/etl-zib:latest
        envFrom:
        - configMapRef:
            name: timezone-config
        - configMapRef:
            name: transform-server-config
        - configMapRef:
            name: zib-templates-config
        - configMapRef:
            name: test-etl-config
        env:
        - name: LOGSTASH_TAGS
          value: ETL_ZIB
        volumeMounts:
        - name: logs
          mountPath: /code/logs/test
        - name: outputs
          mountPath: /code/outputs
        - name: data
          mountPath: /code/data/test-demo-data
        - name: etl
          mountPath: /code
        resources:
          limits:
            memory: "512Mi"
            cpu: "0.5"
          requests:
            memory: "256Mi"
            cpu: "0.2"
      volumes:
      - name: logs
        hostPath:
          path: /mnt/filebeat/logs/test/etl-zib/
          type: Directory
      - name: outputs
        hostPath:
          path: /mnt/outputs/etl-zib
          type: DirectoryOrCreate
      - name: data
        hostPath:
          path: /mnt/data/test-demo-data
          type: Directory
      - name: etl
        hostPath:
          path: /mnt/externals/dh-hdp-etl
          type: Directory
          