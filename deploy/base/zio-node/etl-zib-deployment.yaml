apiVersion: apps/v1
kind: Deployment
metadata:
  name: zio-etl-zib
  namespace: dh-health
  labels:
    app: zio-etl-zib
spec:
  replicas: 1
  selector:
    matchLabels:
      app: zio-etl-zib
  template:
    metadata:
      labels:
        app: zio-etl-zib
    spec:
      initContainers:
      - name: init-zio-etl
        image: docker-health/etl-zib:latest
        imagePullPolicy: Never
        command: ['sh', '-c', 'sleep 70']
      containers:
      - name: zio-etl-zib
        image: docker-health/etl-zib:latest
        envFrom:
        - configMapRef:
            name: timezone-config
        - configMapRef:
            name: transform-server-config
        - configMapRef:
            name: zib-templates-config
        - configMapRef:
            name: zio-etl-config
        volumeMounts:
        - name: outputs
          mountPath: /code/outputs
        - name: data
          mountPath: /code/data/zio-demo-data
        - name: etl-logs
          mountPath: /code/logs/zio
          subPath: zio
      volumes:
      - name: outputs
        persistentVolumeClaim:
            claimName: output-pvc
      - name: data
        hostPath:
          path: /mnt/data/zio-demo-data
          type: Directory
      - name: etl-logs
        persistentVolumeClaim:
          claimName: log-pvc
          