apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: transform-rest-ingress
  annotations:
    nginx.ingress.kubernetes.io/ssl-redirect: "false"
spec:
  ingressClassName: nginx
  rules:
  - host: transform.env.dh.unimaas.nl
    http:
      paths:
      - path: /
        pathType: Prefix
        backend:
          service:
            name: transform-rest
            port:
              number: 8080