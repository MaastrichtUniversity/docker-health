apiVersion: v1
kind: Service
metadata:
  name: federation-api
  namespace: dh-health
spec:
  type: ClusterIP
  ports:
    - port: 8000
      targetPort: 8000
      protocol: TCP
  selector:
    app: federation-api