apiVersion: v1
kind: Service
metadata:
  name: mumc-ehrdb
  namespace: dh-health
spec:
  selector:
    app: mumc-ehrdb
  ports:
  - port: 5432
    targetPort: 5432
    name: postgres
  type: ClusterIP