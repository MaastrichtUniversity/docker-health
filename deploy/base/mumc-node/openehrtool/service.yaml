apiVersion: v1
kind: Service
metadata:
  name: mumc-openehrtool
  namespace: dh-health
spec:
  selector:
    app: mumc-openehrtool
  ports:
  - port: 9000
    targetPort: 9000
    name: http
  type: ClusterIP
