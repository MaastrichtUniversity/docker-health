apiVersion: apps/v1
kind: Deployment
metadata:
  name: mumc-portal
  namespace: dh-health
  labels:
    app: mumc-portal
spec:
  replicas: 1
  selector:
    matchLabels:
      app: mumc-portal
  template:
    metadata:
      labels:
        app: mumc-portal
    spec:
      containers:
      - name: mumc-portal
        image: docker-health/portal:latest
        ports:
        - containerPort: 80
        envFrom:
        - configMapRef:
            name: mumc-portal-config
        - configMapRef:
            name: portal-common-config
        resources:
          limits:
            memory: "512Mi"
            cpu: "0.5"
          requests:
            memory: "128Mi"
            cpu: "0.1"