apiVersion: apps/v1
kind: Deployment
metadata:
  name: mumc-portal
  namespace: dh-health
  labels:
    app: mumc-portal
spec:
  replicas: 1
  selector:
    matchLabels:
      app: mumc-portal
  template:
    metadata:
      labels:
        app: mumc-portal
    spec:
      containers:
      - name: mumc-portal
        image: docker-health/portal:latest
        ports:
        - containerPort: 3000
        envFrom:
        - configMapRef:
            name: mumc-portal-config
        - configMapRef:
            name: portal-common-config