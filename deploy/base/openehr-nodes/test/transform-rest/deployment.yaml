apiVersion: apps/v1
kind: Deployment
metadata:
  name: transform-rest-test
  namespace: dh-health
  labels:
    app: transform-rest-test
spec:
  selector:
    matchLabels:
      app: transform-rest-test
  template:
    metadata:
      labels:
        app: transform-rest-test
    spec:
      containers:
      - name: transform-rest
        image: registry.prod.dh.unimaas.nl/docker-health/transform-rest:latest
        ports:
        - containerPort: 8080
        env:
          - name: TRANSFORM_SPRING_USERNAME
            valueFrom:
              secretKeyRef:
                name: test-transform-creds
                key: username
          - name: TRANSFORM_SPRING_PASSWORD
            valueFrom:
              secretKeyRef:
                name: test-transform-creds
                key: password
        envFrom:
        - configMapRef:
            name: timezone-config
        - configMapRef:
            name: test-transform-server-config
        - configMapRef:
            name: zib-templates-config
