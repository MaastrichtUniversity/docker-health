apiVersion: v1
kind: Service
metadata:
  name: envida-openehrtool
  namespace: dh-health
spec:
  selector:
    app: envida-openehrtool
  ports:
  - port: 9000
    targetPort: 9000
    name: http
  type: ClusterIP