apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

generatorOptions:
  disableNameSuffixHash: true

components:
- ../../../components/federation-rest

resources:
- configmap.yaml

patches:
- path: deployment-volumes.yaml
