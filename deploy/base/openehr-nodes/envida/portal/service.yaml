apiVersion: v1
kind: Service
metadata:
  name: envida-portal
  namespace: dh-health
spec:
  selector:
    app: envida-portal
  ports:
  - port: 3000
    targetPort: 3000
    name: http
  type: ClusterIP