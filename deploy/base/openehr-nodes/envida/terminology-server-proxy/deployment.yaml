apiVersion: apps/v1
kind: Deployment
metadata:
  name: envida-terminology-server-proxy
  namespace: dh-health
  labels:
    app: envida-terminology-server-proxy
spec:
  selector:
    matchLabels:
      app: envida-terminology-server-proxy
  template:
    metadata:
      labels:
        app: envida-terminology-server-proxy
    spec:
      containers:
      - name: envida-terminology-server-proxy
        image: registry.prod.dh.unimaas.nl/docker-health/terminology-server-proxy:latest
        ports:
        - containerPort: 8080
        envFrom:
          - configMapRef:
              name: envida-terminology-server-proxy-config
          - configMapRef:
              name: terminology-server-proxy-config
          - configMapRef:
              name: dry-run-config
          - configMapRef:
              name: timezone-config
        env:
          - name: TERMINOLOGY_USERNAME
            valueFrom:
              secretKeyRef:
                name: terminology-server-proxy-creds
                key: username
          - name: TERMINOLOGY_PASSWORD
            valueFrom:
              secretKeyRef:
                name: terminology-server-proxy-creds
                key: password
        volumeMounts:
          - name: terminology-logs
            mountPath: /opt/app/logs/envida
            subPath: envida/terminology
      volumes:
        - name: terminology-logs
          persistentVolumeClaim:
            claimName: log-pvc