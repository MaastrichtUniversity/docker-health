apiVersion: v1
kind: Service
metadata:
  name: mumc-transform-rest
spec:
  type: ClusterIP
  ports:
    - port: 8080
      targetPort: 8080
      protocol: TCP
  selector:
    app: mumc-transform-rest
