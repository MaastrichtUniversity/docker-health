apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

generatorOptions:
  disableNameSuffixHash: true

components:
- ../../../components/transform-rest

resources:
- configmap.yaml

patches:
- path: deployment-volumes.yaml
