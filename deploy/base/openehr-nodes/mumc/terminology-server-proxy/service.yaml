apiVersion: v1
kind: Service
metadata:
  name: mumc-terminology-server-proxy
  namespace: dh-health
spec:
  type: ClusterIP
  ports:
    - port: 8080
      targetPort: 8080
      protocol: TCP
  selector:
    app: mumc-terminology-server-proxy