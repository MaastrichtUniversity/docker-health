apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

generatorOptions:
  disableNameSuffixHash: true

components:
- ../../../components/etl-zib

resources:
- etl-config

patches:
- path: etl-zib-pipeline/job-volumes.yaml