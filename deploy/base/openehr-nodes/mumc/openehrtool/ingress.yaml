apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: mumc-openehrtool-ingress
  annotations:
    nginx.ingress.kubernetes.io/ssl-redirect: "false"
spec:
  ingressClassName: nginx
  rules:
  - host: openehrtool.mumc.env.dh.unimaas.nl
    http:
      paths:
      - path: /
        pathType: Prefix
        backend:
          service:
            name: mumc-openehrtool
            port:
              number: 9000
