apiVersion: v1
kind: ConfigMap
metadata:
  name: zio-federation-rest-config
  namespace: dh-health
data:
  LOGSTASH_TAGS: "FEDERATION_REST_ZIO"
  FEDERATION_LOGGER_FILENAME: "logs/federation-rest/federation-rest-zio.log"
  FEDERATION_SERVER_URL: http://zio-federation-rest.dh-health:8000