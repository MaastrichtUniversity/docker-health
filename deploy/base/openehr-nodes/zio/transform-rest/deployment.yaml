apiVersion: apps/v1
kind: Deployment
metadata:
  name: transform-rest-zio
  namespace: dh-health
  labels:
    app: transform-rest-zio
spec:
  selector:
    matchLabels:
      app: transform-rest-zio
  template:
    metadata:
      labels:
        app: transform-rest-zio
    spec:
      containers:
      - name: transform-rest
        image: registry.prod.dh.unimaas.nl/docker-health/transform-rest:latest
        ports:
        - containerPort: 8080
        env:
          - name: TRANSFORM_SPRING_USERNAME
            valueFrom:
              secretKeyRef:
                name: zio-transform-creds
                key: username
          - name: TRANSFORM_SPRING_PASSWORD
            valueFrom:
              secretKeyRef:
                name: zio-transform-creds
                key: password
        envFrom:
        - configMapRef:
            name: timezone-config
        - configMapRef:
            name: zio-transform-server-config
        - configMapRef:
            name: zib-templates-config
