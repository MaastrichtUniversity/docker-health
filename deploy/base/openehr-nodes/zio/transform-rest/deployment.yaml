apiVersion: apps/v1
kind: Deployment
metadata:
  name: zio-transform-rest
  namespace: dh-health
  labels:
    app: zio-transform-rest
spec:
  selector:
    matchLabels:
      app: zio-transform-rest
  template:
    metadata:
      labels:
        app: zio-transform-rest
    spec:
      containers:
      - name: zio-transform-rest
        image: registry.prod.dh.unimaas.nl/docker-health/transform-rest:latest
        ports:
        - containerPort: 8080
        env:
          - name: TRANSFORM_SPRING_USERNAME
            valueFrom:
              secretKeyRef:
                name: zio-transform-creds
                key: username
          - name: TRANSFORM_SPRING_PASSWORD
            valueFrom:
              secretKeyRef:
                name: zio-transform-creds
                key: password
        envFrom:
        - configMapRef:
            name: dry-run-config
        - configMapRef:
            name: timezone-config
        - configMapRef:
            name: zio-transform-rest-config
        - configMapRef:
            name: zib-templates-config
        volumeMounts:
        - name: transform-rest-logs
          mountPath: /var/log/zio
          subPath: zio/transform-rest
      volumes:
      - name: transform-rest-logs
        persistentVolumeClaim:
          claimName: log-pvc
