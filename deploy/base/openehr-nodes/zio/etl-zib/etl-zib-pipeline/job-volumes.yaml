apiVersion: batch/v1
kind: Job
metadata:
  name: etl-zib-pipeline          # must match the Job name in job.yaml
spec:
  template:
    spec:
      # ── container volume mounts ──
      containers:
        - name: etl-zib-pipeline
          volumeMounts:
            - name: outputs
              mountPath: /code/outputs
              subPath: zio/pipeline
            - name: data
              mountPath: /code/data/zio-demo-data
            - name: etl-logs
              mountPath: /code/logs/zio
              subPath: zio/pipeline
      # ── volume definitions ──
      volumes:
        - name: outputs
          persistentVolumeClaim:
            claimName: output-pvc-zio
        - name: data
          hostPath:
            path: /mnt/data/zio-demo-data
            type: Directory
        - name: etl-logs
          persistentVolumeClaim:
            claimName: log-pvc-zio
                                                             