apiVersion: v1
kind: Service
metadata:
  name: zio-terminology-server-proxy
spec:
  type: ClusterIP
  ports:
    - port: 8080
      targetPort: 8080
      protocol: TCP
  selector:
    app: zio-terminology-server-proxy