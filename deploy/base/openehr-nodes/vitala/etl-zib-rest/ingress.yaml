apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: vitala-etl-zib-rest-ingress
  namespace: dh-health
  annotations:
    nginx.ingress.kubernetes.io/ssl-redirect: "false"
spec:
  ingressClassName: nginx
  rules:
  - host: etl.vitala.env.dh.unimaas.nl
    http:
      paths:
      - path: /
        pathType: Prefix
        backend:
          service:
            name: vitala-etl-zib-rest
            port:
              number: 8040
