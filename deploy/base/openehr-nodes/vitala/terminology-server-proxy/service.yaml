apiVersion: v1
kind: Service
metadata:
  name: vitala-terminology-server-proxy
  namespace: dh-health
spec:
  type: ClusterIP
  ports:
    - port: 8080
      targetPort: 8080
      protocol: TCP
  selector:
    app: vitala-terminology-server-proxy