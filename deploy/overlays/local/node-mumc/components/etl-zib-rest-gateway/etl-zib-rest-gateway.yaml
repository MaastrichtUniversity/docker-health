apiVersion: gateway.networking.k8s.io/v1
kind: Gateway
metadata:
  name: mumc-etl-zib-rest-gateway
spec:
  gatewayClassName: traefik
  listeners:
    - name: http
      port: 80
      protocol: HTTP
      hostname: etl.mumc.local.dh.unimaas.nl
---
apiVersion: gateway.networking.k8s.io/v1
kind: HTTPRoute
metadata:
  name: mumc-etl-zib-rest-route
spec:
  parentRefs:
    - name: mumc-etl-zib-rest-gateway
  hostnames:
    - etl.mumc.local.dh.unimaas.nl
  rules:
    - matches:
        - path:
            type: PathPrefix
            value: /
      backendRefs:
        - name: mumc-etl-zib-rest
          port: 8040
