apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
  - ../test-single-node

patches:
  - target:
      kind: Deployment
      name: .*-terminology-server-proxy
    path: ../patches/terminology-server-proxy-env.yaml