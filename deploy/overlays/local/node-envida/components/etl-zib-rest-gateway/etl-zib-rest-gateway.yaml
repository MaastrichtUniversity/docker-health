apiVersion: gateway.networking.k8s.io/v1
kind: Gateway
metadata:
  name: envida-etl-zib-rest-gateway
  namespace: dh-health
spec:
  gatewayClassName: traefik
  listeners:
    - name: https
      port: 443
      protocol: HTTPS
      hostname: etl.envida.local.dh.unimaas.nl
    - name: http
      port: 80
      protocol: HTTP
      hostname: etl.envida.local.dh.unimaas.nl
---
apiVersion: gateway.networking.k8s.io/v1
kind: HTTPRoute
metadata:
  name: envida-etl-zib-rest-route
  namespace: dh-health
spec:
  parentRefs:
    - name: envida-etl-zib-rest-gateway
  hostnames:
    - etl.envida.local.dh.unimaas.nl
  rules:
    - matches:
        - path:
            type: PathPrefix
            value: /
      backendRefs:
        - name: envida-etl-zib-rest
          port: 8040
