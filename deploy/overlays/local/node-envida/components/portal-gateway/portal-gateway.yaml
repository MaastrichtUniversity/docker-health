apiVersion: gateway.networking.k8s.io/v1
kind: Gateway
metadata:
  name: envida-portal-gateway 
spec:
  gatewayClassName: traefik
  listeners:
    - name: http
      port: 80
      protocol: HTTP
      hostname: portal.envida.local.dh.unimaas.nl
---
apiVersion: gateway.networking.k8s.io/v1
kind: HTTPRoute
metadata:
  name: envida-portal-route
spec:
  parentRefs:
    - name: envida-portal-gateway
  hostnames:
    - portal.envida.local.dh.unimaas.nl
  rules:
    - matches:
        - path:
            type: PathPrefix
            value: /
      backendRefs:
        - name: envida-portal
          port: 3000
