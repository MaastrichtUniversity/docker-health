apiVersion: gateway.networking.k8s.io/v1
kind: Gateway
metadata:
  name: test-ehrbase-gateway
spec:
  gatewayClassName: traefik
  listeners:
    - name: http
      port: 80
      protocol: HTTP
      hostname: ehrbase.test.local.dh.unimaas.nl
---
apiVersion: gateway.networking.k8s.io/v1
kind: HTTPRoute
metadata:
  name: test-ehrbase-route
spec:
  parentRefs:
    - name: test-ehrbase-gateway
  hostnames:
    - ehrbase.test.local.dh.unimaas.nl
  rules:
    - matches:
        - path:
            type: PathPrefix
            value: /
      backendRefs:
        - name: test-ehrbase
          port: 8080
