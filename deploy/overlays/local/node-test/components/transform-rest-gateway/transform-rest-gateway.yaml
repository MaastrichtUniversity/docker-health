apiVersion: gateway.networking.k8s.io/v1
kind: Gateway
metadata:
  name: test-transform-rest-gateway
spec:
  gatewayClassName: traefik
  listeners:
    - name: http
      port: 80
      protocol: HTTP
      hostname: transform.test.local.dh.unimaas.nl
---
apiVersion: gateway.networking.k8s.io/v1
kind: HTTPRoute
metadata:
  name: test-transform-rest-route
spec:
  parentRefs:
    - name: test-transform-rest-gateway
  hostnames:
    - transform.test.local.dh.unimaas.nl
  rules:
    - matches:
        - path:
            type: PathPrefix
            value: /
      backendRefs:
        - name: transform-rest
          port: 8080
