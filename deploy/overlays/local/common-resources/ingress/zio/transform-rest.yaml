apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: zio-transform-rest-ingress
  namespace: dh-health
  annotations:
    nginx.ingress.kubernetes.io/ssl-redirect: "false"
spec:
  ingressClassName: nginx
  rules:
  - host: transform.zio.local.dh.unimaas.nl
    http:
      paths:
      - path: /
        pathType: Prefix
        backend:
          service:
            name: zio-transform-rest
            port:
              number: 8080
