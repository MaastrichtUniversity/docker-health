apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
  - ../../../base/common

components:
  - components

patches:
  # imagePullPolicy patches
  - target:
      kind: Deployment
    patch: |-
      - op: replace
        path: /spec/template/spec/containers/0/imagePullPolicy
        value: Never    

# TODO why is this a separete setting and not just in the main ehrbase config
configMapGenerator:
  # Set ADMIN_API_ACTIVE: "true" in all nodes for development purposes for openEHRtool ADMIN methods.
  - name: ehrbase-config
    behavior: merge
    literals:
      - ADMIN_API_ACTIVE=true
