apiVersion: kustomize.config.k8s.io/v1alpha1
kind: Component

generatorOptions:
  disableNameSuffixHash: true

secretGenerator:
  - name: federation-service-creds
    namespace: dh-health
    literals:
      - key=serviceKey