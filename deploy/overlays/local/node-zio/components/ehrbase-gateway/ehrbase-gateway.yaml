apiVersion: gateway.networking.k8s.io/v1
kind: Gateway
metadata:
  name: zio-ehrbase-gateway
spec:
  gatewayClassName: traefik
  listeners:
    - name: http
      port: 80
      protocol: HTTP
      hostname: ehrbase.zio.local.dh.unimaas.nl
---
apiVersion: gateway.networking.k8s.io/v1
kind: HTTPRoute
metadata:
  name: zio-ehrbase-route
spec:
  parentRefs:
    - name: zio-ehrbase-gateway
  hostnames:
    - ehrbase.zio.local.dh.unimaas.nl
  rules:
    - matches:
        - path:
            type: PathPrefix
            value: /
      backendRefs:
        - name: ehrbase
          port: 8080
