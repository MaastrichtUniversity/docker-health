apiVersion: gateway.networking.k8s.io/v1
kind: Gateway
metadata:
  name: vitala-federation-rest-gateway
  namespace: dh-health
spec:
  gatewayClassName: traefik
  listeners:
    - name: https
      port: 443
      protocol: HTTPS
      hostname: federation.vitala.local.dh.unimaas.nl
    - name: http
      port: 80
      protocol: HTTP
      hostname: federation.vitala.local.dh.unimaas.nl
---
apiVersion: gateway.networking.k8s.io/v1
kind: HTTPRoute
metadata:
  name: vitala-federation-rest-route
  namespace: dh-health
spec:
  parentRefs:
    - name: vitala-federation-rest-gateway
  hostnames:
    - federation.vitala.local.dh.unimaas.nl
  rules:
    - matches:
        - path:
            type: PathPrefix
            value: /
      backendRefs:
        - name: vitala-federation-rest
          port: 8000
