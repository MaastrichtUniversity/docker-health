apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: envida-openehrtool-ingress
  namespace: dh-health
  annotations:
    nginx.ingress.kubernetes.io/ssl-redirect: "false"
spec:
  ingressClassName: nginx
  rules:
  - host: openehrtool.envida.local.dh.unimaas.nl
    http:
      paths:
      - path: /
        pathType: Prefix
        backend:
          service:
            name: envida-openehrtool
            port:
              number: 9000