apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: dh-health-jupyter

# namePrefix field prepends a string to the names of all resources, including configMapGenerator and secretGenerator.
namePrefix: envida-

resources:
  - ../../../../base/openehr-nodes/envida/etl-zib/etl-config


components:
  - ../../node-envida/components/secrets