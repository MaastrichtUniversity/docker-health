apiVersion: gateway.networking.k8s.io/v1
kind: Gateway
metadata:
  name: elk-gateway
  namespace: dh-health-ops
spec:
  gatewayClassName: traefik
  listeners:
    - name: http
      port: 80
      protocol: HTTP
      hostname: elk.devops.unimaas.nl
---
apiVersion: gateway.networking.k8s.io/v1
kind: HTTPRoute
metadata:
  name: elk-route
  namespace: dh-health-ops
spec:
  parentRefs:
    - name: elk-gateway
  hostnames:
    - elk.devops.unimaas.nl
  rules:
    - matches:
        - path:
            type: PathPrefix
            value: /
      backendRefs:
        - name: elk
          port: 5601
