apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
namespace: dh-health

generatorOptions:
  disableNameSuffixHash: true

resources:
  - pvc.yaml
  - job.yaml

configMapGenerator:
  - name: git-config
    namespace: dh-health
    literals:
      - GIT_BRANCH=2026.1.3
