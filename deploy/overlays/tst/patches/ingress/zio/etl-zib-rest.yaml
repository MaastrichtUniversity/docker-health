apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: zio-etl-zib-rest-ingress
  namespace: dh-health
  annotations:
    nginx.ingress.kubernetes.io/ssl-redirect: "false"
spec:
  ingressClassName: nginx
  tls:
    - hosts:
        - etl.zio.tst.dh.unimaas.nl
      secretName: etl-zio-tls-secret
  rules:
    - host: etl.zio.tst.dh.unimaas.nl
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: zio-etl-zib-rest
                port:
                  number: 8040
