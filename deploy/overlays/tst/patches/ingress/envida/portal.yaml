apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: envida-portal-ingress
  namespace: dh-health
spec:
  rules:
    - host: portal.envida.tst.dh.unimaas.nl
  tls:
    - hosts:
        - portal.envida.tst.dh.unimaas.nl
      secretName: portal-envida-tls-secret