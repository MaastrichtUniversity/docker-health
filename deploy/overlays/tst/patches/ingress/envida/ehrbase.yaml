apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: envida-ehrbase-ingress
  namespace: dh-health
  annotations:
    nginx.ingress.kubernetes.io/ssl-redirect: "false"
spec:
  ingressClassName: nginx
  tls:
    - hosts:
        - ehrbase.envida.tst.dh.unimaas.nl
      secretName: ehrbase-tls-secret
  rules:
    - host: ehrbase.envida.tst.dh.unimaas.nl
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: envida-ehrbase
                port:
                  number: 8080
