apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: transform-rest-ingress
  namespace: dh-health
spec:
  rules:
    - host: transform.tst.dh.unimaas.nl
  tls:
    - hosts:
        - transform.tst.dh.unimaas.nl
      secretName: transform-tls-secret
