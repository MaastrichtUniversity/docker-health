apiVersion: batch/v1
kind: Job
metadata:
  name: vitala-etl-zib-pipeline
  namespace: dh-health
spec:
  template:
    spec:
      containers:
        - name: vitala-etl-zib-pipeline
          volumeMounts:
            - name: data
              mountPath: /code/data/vitala-demo-data
              $patch: delete
      volumes:
        - name: data
          $patch: delete