apiVersion: batch/v1
kind: Job
metadata:
  name: test-etl-zib-pipeline
  namespace: dh-health
spec:
  template:
    spec:
      containers:
        - name: test-etl-zib-pipeline
          volumeMounts:
            - name: demo-data
              mountPath: /code/data/test-demo-data
              subPath: src/data/test-demo-data
      volumes:
        - name: demo-data
          persistentVolumeClaim:
            claimName: git-repo-pvc
