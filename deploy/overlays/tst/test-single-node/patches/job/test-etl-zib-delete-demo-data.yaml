apiVersion: batch/v1
kind: Job
metadata:
  name: test-etl-zib-pipeline
  namespace: dh-health
spec:
  template:
    spec:
      containers:
        - name: test-etl-zib-pipeline
          volumeMounts:
            - name: data
              mountPath: /code/data/test-demo-data
              $patch: delete
      volumes:
        - name: data
          $patch: delete