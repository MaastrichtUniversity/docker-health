apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
  - vault-auth.yaml
  - jupyter-secrets.yaml
  - envida-secrets.yaml
  - mumc-secrets.yaml
  - vitala-secrets.yaml
  - zio-secrets.yaml

generatorOptions:
  disableNameSuffixHash: true