apiVersion: external-secrets.io/v1
kind: ExternalSecret
metadata:
  name: mumc-federation-key
spec:
  refreshInterval: 24h
  secretStoreRef:
    name: prod-vault.tst
    kind: ClusterSecretStore
  target:
    name: mumc-federation-key
    template:
      engineVersion: v2
      data:
        key: "{{ .mumc_federation_key }}"
  dataFrom:
    - extract:
        key: hdp-env/mumc
---
apiVersion: external-secrets.io/v1
kind: ExternalSecret
metadata:
  name: mumc-federation-break-the-glass-key
spec:
  refreshInterval: 24h
  secretStoreRef:
    name: prod-vault.tst
    kind: ClusterSecretStore
  target:
    name: mumc-federation-break-the-glass-key
    template:
      engineVersion: v2
      data:
        key: "{{ .mumc_federation_break_the_glass_key }}"
  dataFrom:
    - extract:
        key: hdp-env/mumc
