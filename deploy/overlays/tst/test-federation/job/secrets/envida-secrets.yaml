apiVersion: external-secrets.io/v1
kind: ExternalSecret
metadata:
  name: envida-federation-key
spec:
  refreshInterval: 24h
  secretStoreRef:
    name: prod-vault.tst
    kind: ClusterSecretStore
  target:
    name: envida-federation-key
    template:
      engineVersion: v2
      data:
        key: "{{ .envida_federation_key }}"
  dataFrom:
    - extract:
        key: hdp-env/envida
---
apiVersion: external-secrets.io/v1
kind: ExternalSecret
metadata:
  name: envida-federation-break-the-glass-key
spec:
  refreshInterval: 24h
  secretStoreRef:
    name: prod-vault.tst
    kind: ClusterSecretStore
  target:
    name: envida-federation-break-the-glass-key
    template:
      engineVersion: v2
      data:
        key: "{{ .envida_federation_break_the_glass_key }}"
  dataFrom:
    - extract:
        key: hdp-env/envida
