apiVersion: batch/v1
kind: Job
metadata:
  name: zio-etl-zib-pipeline
  namespace: dh-health
spec:
  template:
    spec:
      containers:
        - name: zio-etl-zib-pipeline
          volumeMounts:
            - name: data
              mountPath: /code/data/zio-demo-data
              $patch: delete
      volumes:
        - name: data
          $patch: delete