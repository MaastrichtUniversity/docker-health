apiVersion: gateway.networking.k8s.io/v1
kind: Gateway
metadata:
  name: mumc-portal-gateway
spec:
  gatewayClassName: traefik
  listeners:
    - name: http
      port: 80
      protocol: HTTP
      hostname: portal.mumc.fehr.amumc.nl
---
apiVersion: gateway.networking.k8s.io/v1
kind: HTTPRoute
metadata:
  name: mumc-portal-route
spec:
  parentRefs:
    - name: mumc-portal-gateway
  hostnames:
    - portal.mumc.fehr.amumc.nl
  rules:
    - matches:
        - path:
            type: PathPrefix
            value: /
      backendRefs:
        - name: portal
          port: 3000
