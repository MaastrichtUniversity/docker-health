apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: dh-health-vitala

resources:
  - ../../../base/openehr-nodes/vitala
  - config
  - service-discovery

components:
  - components
  - ../shared-components
