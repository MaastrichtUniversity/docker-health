apiVersion: batch/v1
kind: Job
metadata:
  name: envida-etl-zib-pipeline
  namespace: dh-health
spec:
  template:
    spec:
      containers:
        - name: envida-etl-zib-pipeline
          volumeMounts:
            - name: demo-data
              mountPath: /code/data/envida-demo-data
              subPath: src/data/envida-demo-data
      volumes:
        - name: demo-data
          persistentVolumeClaim:
            claimName: git-repo-pvc
