apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: vitala-ehrbase-ingress
  namespace: dh-health
  annotations:
    nginx.ingress.kubernetes.io/ssl-redirect: "false"
spec:
  ingressClassName: nginx
  rules:
    - host: ehrbase.vitala.fehr.mumc.nl
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: vitala-ehrbase
                port:
                  number: 8080
