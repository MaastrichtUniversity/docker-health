apiVersion: batch/v1
kind: Job
metadata:
  name: etl-zib-pipeline
  namespace: dh-health-vitala
spec:
  template:
    spec:
      containers:
        - name: etl-zib-pipeline
          volumeMounts:
            - name: data
              mountPath: /code/data/vitala-demo-data
              $patch: delete
      volumes:
        - name: data
          $patch: delete
