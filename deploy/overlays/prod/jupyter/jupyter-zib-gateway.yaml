apiVersion: gateway.networking.k8s.io/v1
kind: Gateway
metadata:
  name: jupyter-gateway
spec:
  gatewayClassName: traefik
  listeners:
    - name: http
      port: 80
      protocol: HTTP
      hostname: jupyter.fehr.mumc.nl
---
apiVersion: gateway.networking.k8s.io/v1
kind: HTTPRoute
metadata:
  name: jupyter-zib-route
spec:
  parentRefs:
    - name: jupyter-gateway
  hostnames:
    - jupyter.fehr.mumc.nl
  rules:
    - matches:
        - path:
            type: PathPrefix
            value: /
      backendRefs:
        - name: jupyter-zib
          port: 8888
