apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: dh-health-zio

resources:
  - ../../../base/openehr-nodes/zio
  - config
  - service-discovery

components:
  - components
  - ../shared-components
